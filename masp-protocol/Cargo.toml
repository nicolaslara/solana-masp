[package]
name = "masp-protocol"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Shared protocol types and constants for MASP (no_std compatible)"

[features]
default = ["std"]
std = ["borsh/std"]

[dependencies]
borsh = { version = "1.5", default-features = false, features = ["derive"] }
