[package]
name = "solana-masp"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Multi-Asset Shielded Pool for Solana (scaffolding)"

[lib]
crate-type = ["cdylib", "lib"]

[dependencies]
solana-program = "2.0"

[features]
default = []
no-entrypoint = []

## NOTE:
## Profile settings must live at the workspace root `Cargo.toml`.
